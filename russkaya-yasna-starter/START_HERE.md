# 🚀 НАЧНИ ОТСЮДА

## Что это?

Это стартовый шаблон проекта **"Русская Ясна"** для работы с Cursor AI.

---

## 📋 Чек-лист перед началом

### 1. ✅ Загрузи в GitHub

```bash
# В папке проекта выполни:
git init
git add .
git commit -m "Initial commit: Project starter"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/russkaya-yasna.git
git push -u origin main
```

### 2. ✅ Склонируй к себе

```bash
git clone https://github.com/YOUR_USERNAME/russkaya-yasna.git
cd russkaya-yasna
```

### 3. ✅ Установи зависимости

```bash
npm install
```

Это установит:
- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- Framer Motion
- React Hook Form
- Zod
- Lucide React

### 4. ✅ Запусти dev-сервер

```bash
npm run dev
```

Открой http://localhost:3000 - должна появиться стартовая страница.

### 5. ✅ Открой Cursor

```bash
cursor .
# или просто открой папку в Cursor
```

---

## 🎯 Что дальше?

### Теперь используй промпты!

В папке `docs/` лежит файл **`CURSOR_PROMPTS.md`** с 17 промптами для создания сайта.

**Начни с ПРОМПТА 1** - открой Cursor Agent и скопируй первый промпт.

---

## 📁 Структура проекта

```
russkaya-yasna/
├── app/                    # ✅ Next.js App Router (готово)
│   ├── layout.tsx         # Root layout с метаданными
│   ├── page.tsx           # Стартовая страница
│   └── globals.css        # Глобальные стили + Tailwind
├── components/            # Папки для компонентов (пустые, заполнишь по промптам)
│   ├── ui/               
│   ├── sections/         
│   └── layout/           
├── lib/                   # Утилиты и данные (заполнишь по промптам)
├── public/               # Статические файлы
│   └── images/          
├── docs/                  # 📚 Документация
│   └── CURSOR_PROMPTS.md # ПРОМПТЫ ДЛЯ CURSOR
├── .gitignore            # ✅ Готово
├── package.json          # ✅ Готово
├── tsconfig.json         # ✅ Готово
├── tailwind.config.ts    # ✅ Готово (с кастомными цветами)
├── postcss.config.mjs    # ✅ Готово
├── next.config.mjs       # ✅ Готово
└── README.md             # ✅ Готово
```

---

## 🎨 Кастомные цвета уже настроены

В `tailwind.config.ts` настроены цвета бренда:

- **Primary** (синий): `text-primary`, `bg-primary-500`, etc.
- **Secondary** (золотой): `text-secondary`, `bg-secondary-400`, etc.
- **Accent** (яркий синий): `text-accent`, `bg-accent`, etc.

Используй их в Tailwind классах!

---

## 🔧 Полезные команды

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для production
npm run start    # Запуск production сервера
npm run lint     # Проверка кода ESLint
```

---

## ✨ Что уже работает?

✅ Next.js 14 с App Router  
✅ TypeScript (strict mode)  
✅ Tailwind CSS с кастомными цветами  
✅ Шрифты: Inter (body) + Playfair Display (заголовки)  
✅ ESLint настроен  
✅ Git репозиторий готов  
✅ Базовая структура папок  

---

## 🚀 Начинай создавать!

1. Открой `docs/CURSOR_PROMPTS.md`
2. Найди **ПРОМПТ 1**
3. Скопируй в Cursor Agent
4. Следуй инструкциям

**Удачи!** 💪

---

## 📞 Если что-то не работает

### npm install выдает ошибку?
```bash
rm -rf node_modules package-lock.json
npm install
```

### Dev-сервер не запускается?
Проверь, что порт 3000 свободен:
```bash
lsof -ti:3000 | xargs kill  # macOS/Linux
```

### TypeScript ругается?
```bash
npm run lint
```

Если не помогло - напиши в issues на GitHub.
